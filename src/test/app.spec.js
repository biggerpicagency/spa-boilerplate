describe("Main application runner", function() {
    angular.module('config', []);
    angular.module('Home', []);
    
    var rootScope, state, stateParams, ENV;

    beforeEach(function () {
        module('app');
    });
});